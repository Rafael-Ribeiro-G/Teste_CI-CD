name: Compilar_Python
on: [push]
jobs:
  Develop:
    runs-on: ubuntu-latest
    steps:
      - name: Obter o codigo
        uses: action/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v6.2.0
